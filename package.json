{
  "name": "escolha-certa-landing-page",
  "version": "1.0.0",
  "description": "Landing page para Escolha Certa Contábil",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p dist && cp *.html dist/ && cp *.css dist/ && cp *.js dist/ && cp _headers dist/ && cp _redirects dist/ && cp robots.txt dist/ && cp sitemap.xml dist/",
    "start": "npx serve . -p 8000"
  },
  "keywords": ["landing-page", "contabilidade", "static-site"],
  "author": "Manoel Neto",
  "license": "MIT",
  "devDependencies": {},
  "dependencies": {}
}